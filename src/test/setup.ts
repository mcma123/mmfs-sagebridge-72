// Minimal test setup for Vitest with jsdom
// Can be extended if needed for global stubs

// Ensure localStorage is available (provided by jsdom)
// No-op: jsdom provides window.localStorage

export {}